# Docker Compose environment variables
# Copy this file to .env and fill in the values
POSTGRES_PASSWORD=ChangeMe123

# Local (Docker) Redis connection — matches docker-compose.yml redis service
# redis/redis-stack runs without a password by default.
REDIS_HOST=127.0.0.1
REDIS_PORT=6379